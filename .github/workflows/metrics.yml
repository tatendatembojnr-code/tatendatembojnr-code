name: Metrics
on:
  schedule: [{cron: "0 0 * * *"}]
  workflow_dispatch:
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}

          # Core Settings
          user: tatendatembojnr-code
          template: classic
          base: header, activity, community, repositories, metadata
          base_indepth: yes
          config_timezone: Africa/Harare
          
          # Custom Color Styling (Applying your hex codes)
          config_theme: dark
          config_custom_styles: |
            :root {
              --color-calendar-graph-day-bg: #000000;
              --color-calendar-graph-day-L1-bg: #042b6b;
              --color-calendar-graph-day-L2-bg: #043382;
              --color-calendar-graph-day-L3-bg: #0a4fbc;
              --color-calendar-graph-day-L4-bg: #ffffff;
            }
            svg { background-color: #000000; }
            h1, h2, h3, p, text { fill: #ffffff !important; }

          # 📊 Languages Plugin (More detailed)
          plugin_languages: yes
          plugin_languages_analysis_timeout: 15
          plugin_languages_categories: markup, programming
          plugin_languages_colors: github
          plugin_languages_limit: 8
          plugin_languages_recent_categories: markup, programming
          plugin_languages_recent_days: 14
          plugin_languages_recent_load: 300
          plugin_languages_sections: most-used
          plugin_languages_threshold: 0%

          # 🔥 Coding Habits & Streaks
          plugin_habits: yes
          plugin_habits_facts: yes
          plugin_habits_charts: yes
          plugin_habits_days: 14
          plugin_habits_trim: yes

          # 🏆 Achievements & Badges
          plugin_achievements: yes
          plugin_achievements_display: compact
          plugin_achievements_secrets: yes
          plugin_achievements_threshold: C

          # 📈 Lines of Code
          plugin_lines: yes
          plugin_lines_history_limit: 1
          plugin_lines_repositories_limit: 4
          plugin_lines_sections: base

          # 📅 Isometric Commit Calendar (Looks amazing)
          plugin_isocalendar: yes
          plugin_isocalendar_duration: half-year
